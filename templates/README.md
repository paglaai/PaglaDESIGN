# templates

Reusable page-level compositions.

Templates are not components. They compose components into page-level layouts: landing page, product page, blog post, documentation, case study, hero.

Components live in `components/`; templates live here.
